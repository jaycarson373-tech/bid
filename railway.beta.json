{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile.beta" },
  "deploy": { "restartPolicyType": "NEVER", "numReplicas": 1 }
}
