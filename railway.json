{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile.liquidity" },
  "deploy": {
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
